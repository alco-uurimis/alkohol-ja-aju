import { useCallback,useEffect,useRef,useState } from 'react';
import { Section,ExerciseNote,ResultCard,SourceReference } from '../components/Shared';
import { attentionRound,scoreSelection,toggleSelection } from '../utils/tasks';
import { science } from '../data/content';
import { useStageFocus } from '../hooks/useStageFocus';
export default function Attention(){
 const [stage,setStage]=useState<'intro'|'play'|'result'>('intro');const [grid,setGrid]=useState(attentionRound);const [selected,setSelected]=useState<number[]>([]);const [timed,setTimed]=useState(true);const [remaining,setRemaining]=useState(45);const [elapsed,setElapsed]=useState(0);const [cursor,setCursor]=useState(0);const started=useRef(0);const ended=useRef(false);const cells=useRef<(HTMLButtonElement|null)[]>([]);const heading=useStageFocus(stage);
 const finish=useCallback(()=>{if(ended.current)return;ended.current=true;setElapsed(Math.min(timed?45:Infinity,(performance.now()-started.current)/1000));setStage('result');},[timed]);
 useEffect(()=>{if(stage!=='play'||!timed)return;const tick=()=>{const left=Math.max(0,Math.ceil(45-(performance.now()-started.current)/1000));setRemaining(left);if(!left)finish();};const id=window.setInterval(tick,100);tick();return()=>clearInterval(id);},[stage,timed,finish]);
 const start=()=>{setGrid(attentionRound());setSelected([]);setCursor(0);setRemaining(45);setElapsed(0);ended.current=false;started.current=performance.now();setStage('play');};
 const select=(i:number)=>{if(ended.current)return;if(timed&&performance.now()-started.current>=45000){finish();return;}setSelected(s=>toggleSelection(s,i));};
 const targets=grid.flatMap((c,i)=>c==='T'?[i]:[]);const result=scoreSelection(targets,selected);
 return <Section id="tahelepanu" number="03 / MÄRKA" title="Kui tähelepanelik sa oled?" intro="Leia sarnaste tähtede seast kõik T-tähed. Millised märgid tõmbavad sinu tähelepanu?"><div className="exercise-layout"><aside><span className="pill">TÄHELEPANUHARJUTUS</span><h3>Oluline eristub taustast.</h3><p>Ruudustikus on 36 tähte. Vali kõik T-tähed ja lõpeta, kui oled valmis. Valikut saab uuesti vajutades tühistada.</p><p className="small">Klaviatuuril liigu nooltega. Enter või tühik valib tähe. Tab viib ruudustikust välja. Iga rea algusesse ja lõppu saab klahvidega Home ja End.</p><ExerciseNote/></aside><div className="task-card"><div className="task-top"><span>OTSI T-TÄHTI</span><span role="timer" aria-label="Järelejäänud aeg">{timed?`${remaining} s`:'Ajapiiranguta'}</span></div><h3 ref={heading} tabIndex={-1}>{stage==='intro'?'Leia sihtmärgid':stage==='play'?'Vali kõik T-tähed':'Sinu tähelepanuülesande tulemus'}</h3>{stage==='intro'&&<><div className="target-demo" aria-hidden="true"><span>L</span><span className="demo-target">T</span><span>I</span></div><p>Aega on 45 sekundit. Soovi korral võta ajapiirang maha, et liikuda endale sobivas tempos.</p><label className="check-label"><input type="checkbox" checked={!timed} onChange={e=>setTimed(!e.target.checked)}/> Harjutan ilma ajapiiranguta</label><button className="button primary" onClick={start}>Alusta tähelepanuharjutust <span aria-hidden="true">→</span></button></>}{stage==='play'&&<><div className="letter-grid" role="group" aria-label="Täheruudustik, 6 rida ja 6 veergu">{grid.map((c,i)=><button key={i} ref={el=>{cells.current[i]=el;}} tabIndex={cursor===i?0:-1} onFocus={()=>setCursor(i)} aria-label={`Rida ${Math.floor(i/6)+1}, veerg ${i%6+1}: ${c}`} aria-pressed={selected.includes(i)} className={'letter '+(selected.includes(i)?'selected':'')} onClick={()=>select(i)} onKeyDown={e=>{let next=i;switch(e.key){case 'ArrowRight':next=Math.min(35,i+1);break;case 'ArrowLeft':next=Math.max(0,i-1);break;case 'ArrowDown':next=Math.min(35,i+6);break;case 'ArrowUp':next=Math.max(0,i-6);break;case 'Home':next=i-i%6;break;case 'End':next=i-i%6+5;break;default:return;}e.preventDefault();setCursor(next);cells.current[next]?.focus();}}>{c}</button>)}</div><p className="small" role="status">Valitud tähti: {selected.length}{timed&&remaining<=10?' · Aega on alla 11 sekundi.':''}</p><button className="button primary" onClick={finish}>Lõpeta ja vaata tulemust</button><button className="text-button cancel" onClick={()=>{ended.current=true;setStage('intro');}}>Katkesta harjutus</button></>}{stage==='result'&&<><ResultCard score={result.hits} total={12} title="Leitud T-tähed"><p>{timed&&elapsed>=45?'Aeg sai täis. ':''}Kestus: {elapsed.toFixed(1).replace('.',',')} s · {timed?'Ajapiiranguga':'Ajapiiranguta'}</p></ResultCard><div className="stats"><div><strong>{result.falseAlarms}</strong><span>Ekslikke valikuid</span></div><div><strong>{result.missed}</strong><span>Leidmata T-tähti</span></div><div><strong>{result.accuracy}%</strong><span>Valikute täpsus</span></div></div><p className="small">Täpsus = õiged valikud / kõik valikud. Valikute puudumisel on täpsus 0%. Koos leidmata sihtmärkidega näitab see, kuidas ülesanne läks.</p><details><summary>Vaata sihtmärke ja valikuid</summary><div className="letter-grid review-grid">{grid.map((c,i)=><span className={c==='T'?'target-result':''} key={i}>{c}<small>{selected.includes(i)?'valitud':'—'}</small></span>)}</div><p className="small">Rohelise taustaga on kõik T-tähed.</p></details><p>{science.attention}</p><p>{science.attentionAlcohol} <SourceReference ids={[3]}/></p><button className="button primary" onClick={()=>setStage('intro')}>Proovi tähelepanuharjutust uuesti</button></>}</div></div></Section>;
}
